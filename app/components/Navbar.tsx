export default function Navbar(){
    return(
        <nav className="flex justify-between">
            <div>
                <Image src={} />
            </div>
            <button>RSVP</button>
        </nav>
    )
}